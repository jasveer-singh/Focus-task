# Vercel Postgres pooled connection string
DATABASE_URL="postgres://USER:PASSWORD@HOST:PORT/DB?pgbouncer=true&connect_timeout=15"

# Vercel Postgres direct/non-pooled connection string (for migrations)
DIRECT_URL="postgres://USER:PASSWORD@HOST:PORT/DB"

AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:3000"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
